<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.playposse.thomas.lindenmayer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name="com.playposse.thomas.lindenmayer.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.playposse.thomas.lindenmayer.RulesActivity"
            android:parentActivityName="com.playposse.thomas.lindenmayer.MainActivity"
            android:launchMode="singleTop" />
        <activity
            android:name="com.playposse.thomas.lindenmayer.RenderingActivity"
            android:launchMode="singleTop"
            android:parentActivityName="com.playposse.thomas.lindenmayer.RulesActivity" />
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.playposse.thomas.lindenmayer"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name="com.playposse.thomas.lindenmayer.HelpActivity"></activity>
    </application>

</manifest>